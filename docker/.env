# ========================================
# INVENTRAK STORE - ENVIRONMENT CONFIG
# ========================================

# --- AWS RDS Database Credentials ---
DB_HOST=thinturedb.c3oqsa2ui3r2.ap-south-1.rds.amazonaws.com
DB_PORT=3306
DB_NAME=stock
DB_USER=admin
DB_PASSWORD=Thinture24

# --- Local Database Fallback (Optional) ---
# Only needed if you are still running the local "store-db" container
DB_ROOT_PASSWORD=RootThinture24

# --- Backend Configuration ---
SPRING_PROFILES_ACTIVE=prod
SERVER_PORT=3001

# --- Security / JWT ---
#JWT_SECRET=YourDefaultDevSecretKeyThatIsAtLeast256BitsLongForHS256Algorithm123456789
# JWT_EXPIRATION=86400000