# ============================================
# DEV PROFILE - Local Development
# ============================================
# Activate: spring.profiles.active=dev (or set SPRING_PROFILES_ACTIVE=dev)

# Local MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/stock?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=root

# Show SQL in dev
spring.jpa.show-sql=true

# Dev JWT secret (don't use in production!)
jwt.secret=devSecretKeyForInventoryManagementSystemVeryLongAndSecure12345

# Debug logging
logging.level.com.company.inventory=DEBUG
